language: node_js
sudo: required
services:
  - docker

before_install:
- docker pull node:6-alphine

script:
- docker build -t="404busters/builder" .
- docker run --name builder -it --rm -v $PWD/public:/app/public 404busters/builder
